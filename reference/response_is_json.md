# checks if the response is json or not by looking at the responses headers

checks if the response is json or not by looking at the responses
headers

## Usage

``` r
response_is_json(x)
```

## Arguments

- x:

  A response

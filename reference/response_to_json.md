# converts response to json by first converting the response to text

converts response to json by first converting the response to text

## Usage

``` r
response_to_json(x)
```

## Arguments

- x:

  A response

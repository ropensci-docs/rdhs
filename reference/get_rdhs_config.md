# Get rdhs config

Gets the rdhs config being used

## Usage

``` r
get_rdhs_config()
```

## Value

A `data.frame` containing your rdhs config

## Details

Returns the config being used by rdhs at the moment. This will either be
a \`data.frame\` with class \`rdhs_config\` or will be NULL if this has
not been set up yet
